from PySide6.QtWidgets import (
    QWidget,
    QVBoxLayout,
    QFormLayout,
    QLabel,
    QGroupBox,
    QScrollArea,
)


class HomePage(QWidget):        
    def __init__(self, parent=None):
        super().__init__(parent)
        self._build_gui()

    def _build_gui(self):
        outer = QVBoxLayout(self)
        outer.setContentsMargins(0, 0, 0, 0)

        scroll = QScrollArea()
        scroll.setWidgetResizable(True)
        scroll.setFrameShape(QScrollArea.NoFrame)

        content = QWidget()
        root_layout = QVBoxLayout(content)
        root_layout.setContentsMargins(16, 16, 16, 16)
        root_layout.setSpacing(12)

        group_main_info = QGroupBox('Основная информация о программе')
        form_main_info = QFormLayout(group_main_info)
        self.program_name_and_version = QLabel()
        self.program_name_and_version.setWordWrap(True)
        self.program_name_and_version.setText(
            'GRDH v1.2.0 - guide RNA design helper\n\n' \
            'Программа для поиска, подбора и анализа направляющих РНК (нРНК) для системы CRISPR/Cas9. Работает локально, без подключения к серверам и без очередей на выполнение.'
        )
        form_main_info.addRow(self.program_name_and_version)

        group_features = QGroupBox('Возможности')
        form_features = QFormLayout(group_features)
        self.features_text = QLabel()
        self.features_text.setWordWrap(True)
        self.features_text.setText(
            'Источники входной последовательности:\n' \
            '• .fasta-файл с опциональным указанием границ региона\n' \
            '• Собственная последовательность, введённая вручную\n' \
            '• ID локуса в базе NCBI nucleotide с опциональными границами\n' \
            '• Выдача инструмента CRISPOR (.xls) - анализируется наравне с локальным поиском\n\n' \
            'Скоринг и оценка нРНК:\n' \
            '• Doench 16 (Azimuth) - эффективность разреза, модель извлечена из оригинального Azimuth и воспроизводит его результаты\n' \
            '• Собственный OTS (Our Total Score) -  балл, учитывающий GC-состав, олигонуклеотидные повторы,\n' \
            'нуклеотиды в ключевых позициях спейсера (3, 14, 16, 18, 20), наличие GCC на участке 16–20, доступность спейсера во вторичной структуре (положения 18–20 и 51–53),\n' \
            'наличие 8 и более спаренных нуклеотидов подряд и более 12 спаренных всего, а также N-позицию PAM\n' \
            '• Поиск сайтов рестрикции вблизи точки разреза с подсчётом их количества в радиусе 100, 250 и 500 п.н.\n\n' \
            'Вторичная структура нРНК:\n' \
            '• Собственная отрисовка вторичной структуры на основе точечно-скобочной структуры. С помощью библиотеки ViennaRNA расчитывается точечно-скобочная структура и MFE\n' \
            '• Расчёт доступности ключевых позиций спейсера\n' \
            '• Сохранение изображения структуры для каждой нРНК\n\n' \
            'Интерактивная геномная карта:\n' \
            '• Координатный зум\n' \
            '• Стрелки, отображающие направление нРНК (sense/antisense), масштабируемые вместе с зумом\n' \
            '• Красная линия разреза Cas9, PAM вынесен рядом со спейсером\n' \
            '• Автоматическая раскладка перекрывающихся нРНК по дорожкам\n' \
            '• Всплывающие подсказки с параметрами нРНК при наведении\n\n' \
            'Работа с таблицей нРНК:\n' \
            '• Двусторонняя синхронизация: выбор нРНК на карте выделяет строку в таблице и наоборот\n' \
            '• Удаление нРНК из таблицы с автоматическим обновлением карты\n' \
            '• Подсветка ячеек по критериям качества: Doench < 50, T в N-позиции PAM, олиго-T, GCC на 16–20, более 8 спаренных подряд, более 12 спаренных всего\n' \
            '• Сортировка по любому столбцу\n\n' \
            'Режим подбора пары нРНК:\n' \
            '• Выбор нРНК-якоря с автоматической фильтрацией остальных по расстоянию между точками разреза (50–2000 п.н.)\n' \
            '• Подходит для экспериментов с парными никазами и делеций\n' \
            '• Кнопка сброса фильтра для возврата к полному списку\n\n' \
            'Экспорт:\n' \
            '• Экспорт оставленных в таблице нРНК в формат .xlsx с изображениями структур\n' \
            '• Все изображения структур текущей сессии сохраняются в папке output'
        )
        form_features.addRow(self.features_text)

        group_advantages = QGroupBox('Отличия от других инструментов')
        form_advantages = QFormLayout(group_advantages)
        self.advantages_text = QLabel()
        self.advantages_text.setWordWrap(True)
        self.advantages_text.setText(
            'Сравнение проведено с инструментами CRISPOR, CRISPR-P 2.0, CRISPRon, CRISPR DB, sgDesigner и RNAfold (ViennaRNA).\n\n' \
            'Уникальные возможности GRDH:\n' \
            '• Подбор пары нРНК по расстоянию между точками разреза (50–2000 п.н.) - недоступен ни в одном из перечисленных инструментов\n' \
            '• Двусторонняя синхронизация карта↔таблица: выбор нРНК на карте выделяет строку в таблице и наоборот, удаление из таблицы обновляет карту\n' \
            '• Комбинация вторичной структуры, интерактивной карты и собственного скоринга в одном инструменте.\n' \
            '• Импорт выдачи CRISPOR (.xls) с последующим анализом вторичной структуры, OTS и отображением на карте - объединение преимуществ CRISPOR и GRDH\n' \
            '• Локальная работа с графическим интерфейсом - CRISPOR и CRISPR-P 2.0 работают только через браузер, sgDesigner и CRISPRon - только через командную строку\n\n' \
            'Сильные стороны GRDH:\n' \
            '• Doench 16 - реализация, воспроизводящая оригинальную модель\n' \
            '• Анализ вторичной структуры нРНК: ViennaRNA рассчитывает точечно-скобочную структуру и MFE, отрисовка 2D-структуры в .png - собственная реализация. Оценка доступности ключевых позиций спейсера.\n' \
            'Из перечисленных инструментов структуру нРНК предсказывает только CRISPR-P 2.0 (использует точечно-скобочный визуализатор ViennaRNA), CRISPRon использует энергию фолдинга без отрисовки, остальные не анализируют структуру вовсе\n' \
            '• Поиск сайтов рестрикции (но без подсчёта их числа в радиусе 100/250/500 п.н). - также есть у CRISPOR и CRISPR-P, отсутствует у остальных\n' \
            '• Интерактивная карта с зумом и визуализацией точки разреза Cas9 - реализован у CRISPR-P 2.0, CRISPOR выводит только целевую последовательность с указанием расположения PAM-сайтов без привязки к таблице\n\n' \
            'Слабые стороны GRDH:\n' \
            '• Off-target анализ не реализован (в планах). CRISPOR использует MIT и CFD, CRISPR-P 2.0 - собственный скоринг. Для оценки off-target в данной версии рекомендуется использовать CRISPOR, выдачу которого GRDH может импортировать\n' \
            '• Поддерживается только SpCas9. CRISPR-P 2.0 и CRISPOR поддерживают другие Cas-системы (в планах)\n' \
            '• Нет расчёта праймеров для клонирования - есть у CRISPOR\n' \
            '• Нет оценки микрогомологии - есть у CRISPR-P 2.0\n' \
            '• OTS - эвристический балл, не обученный на экспериментальных данных. CRISPRon и CRISPR DB вероятно точнее предсказывают эффективность разреза\n' \
            '• Нет привязки к геному - GRDH работает с произвольной последовательностью. CRISPOR и CRISPR-P 2.0 ищут нРНК в контексте выбранного генома'
        )
        form_advantages.addRow(self.advantages_text)

        group_manual = QGroupBox('Руководство')
        form_manual = QFormLayout(group_manual)
        self.manual_text = QLabel()
        self.manual_text.setWordWrap(True)
        self.manual_text.setText(
            '1.\tВыберите режим поиска нРНК: .fasta / собственная последовательность / NCBI / CRISPOR.\n' \
            '2.a\tПри выборе .fasta укажите путь до файла. При необходимости задайте границы региона.\n' \
            '2.b\tПри выборе собственной последовательности вставьте её в поле ввода (только ATGC, не более 5 тыс. п.н.).\n' \
            '2.c\tПри выборе NCBI укажите ID локуса в базе nucleotide. При необходимости задайте границы региона.\n' \
            '2.d\tПри выборе CRISPOR укажите путь до .xls-файла с результатами CRISPOR.\n' \
            '3.\tДля локального поиска длина целевой последовательности должна быть ' \
            'не менее 50 и не более 5 тыс. п.н. При ошибке исправьте значения границ.\n' \
            '4.\tНажмите кнопку «Анализ». При успешном запуске появятся полоса прогресса, счётчик проанализированных нРНК и последовательность текущего спейсера.\n' \
            '5.\tПосле завершения анализа появятся карта и таблица с результатами.\n' \
            ' \tПриближение карты колесом мыши позволяет увидеть отдельные нуклеотиды. Красной чертой показано место разреза Cas9, PAM отображается рядом со спейсером. Наведение на спейсер показывает его параметры.\n' \
            ' \tКлик по нРНК на карте выделяет соответствующую строку в таблице. Выбор строки в таблице подсвечивает нРНК на карте.\n' \
            '6.\tТаблица содержит основную информацию о нРНК. Изначально она отсортирована по Doench 16, значения ниже 50 отмечаются красным.\n' \
            ' \tЯчейки Sequence и PAM подсвечиваются при наличии: T в N-позиции PAM (NGG), более 8 спаренных нуклеотидов подряд и более 12 спаренных всего в спейсере, GCC в положениях 16–20, четырёх T подряд.\n' \
            ' \tМожно удалить выбранные нРНК или все, отмеченные красным - карта обновится автоматически.\n' \
            '7.\tДля подбора пары нРНК нажмите кнопку «Подбор пары», затем кликните по нРНК на карте. Остаются только нРНК с точкой разреза в диапазоне 50–2000 п.н. от выбранной. Кнопка «Сбросить фильтр» возвращает полный список.\n' \
            '8.\tЭкспорт в формат .xlsx осуществляется только для оставленных в таблице нРНК. Все изображения структур текущей сессии хранятся в папке output.'
        )
        form_manual.addRow(self.manual_text)

        group_plans = QGroupBox('Планы')
        form_plans = QFormLayout(group_plans)
        self.future_plans = QLabel()
        self.future_plans.setWordWrap(True)
        self.future_plans.setText(
            'Чтение целевой последовательности из .gb-файлов и добавление аннотаций из .gb в геномную карту\n' \
            'Поиск off-target по выбранному геному (.fasta, .gb, NCBI ID)\n' \
            'Включение Doench 14 в скоринг\n' \
            'Расширение списка доступных Cas-систем и скоринга под них\n' \
            'Перевод на английский язык, добавление возможности сменить локализацию\n' \
            'Оптимизация и валидация баллов OTS'
        )
        form_plans.addRow(self.future_plans)

        group_authors = QGroupBox('Авторы')
        form_authors = QFormLayout(group_authors)
        self.authors = QLabel()
        self.authors.setWordWrap(True)
        self.authors.setText(
            'Лукин А.Д. - разработка и поддержка\n' \
            'Франкевич Т.А. - тестирование, биологическая экспертиза\n' \
            'Пермякова Н.В. - научное руководство, концепция, валидация критериев отбора, биологическая экспертиза,требования к реализации\n\n' \
            'Лаборатория биоинженерии растений ИЦИГ СО РАН\n\n' \
            'Авторы выражают благодарность Марковой С.А. за предоставление иконок'
        )
        form_authors.addRow(self.authors)

        root_layout.addWidget(group_main_info)
        root_layout.addWidget(group_features)
        root_layout.addWidget(group_advantages)
        root_layout.addWidget(group_manual)
        root_layout.addWidget(group_plans)
        root_layout.addWidget(group_authors)
        root_layout.addStretch()

        scroll.setWidget(content)
        outer.addWidget(scroll)